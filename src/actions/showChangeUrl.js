/**
 * The redux action to show the change url view
 * - Currently using exports, until we get electron using es6
 *
 * @return {Object} Redux action to dispatch, containing type and optional
 * payload
 */
module.exports = () => ({ type: 'SHOW_CHANGE_URL' });
